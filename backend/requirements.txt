# Backend API core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=1.10,<2
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.0  # For calling AI Engine (port 8001) via REST API

# Auth (JWT: used by app.core.dependencies for /api/patients)
PyJWT>=2.0.0

# Test dependencies
pytest==8.2.0
pytest-asyncio==0.23.6

# Supabase + auth
supabase
email-validator
python-jose[cryptography]

# NOTE: AI-related dependencies (torch, transformers, openai, pyannote, whisper) moved to AI Engine
# Backend communicates with AI Engine via REST API + shared secret token
# See: ai_engine/requirements.txt for full AI stack
